import { XAxisTickFormattingPipe } from './x-axis-tick-formatting.pipe';

describe('XAxisTickFormattingPipe', () => {
  it('create an instance', () => {
    const pipe = new XAxisTickFormattingPipe();
    expect(pipe).toBeTruthy();
  });
});
