import { StarAveragePipe } from './star-average.pipe';

describe('StarAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new StarAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
