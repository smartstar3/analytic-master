import { ChartOptionAvailablePipe } from './chart-option-available.pipe';

describe('ChartOptionAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new ChartOptionAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
