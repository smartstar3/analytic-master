import { ChartIconPipe } from './chart-icon.pipe';

describe('ChartIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartIconPipe();
    expect(pipe).toBeTruthy();
  });
});
